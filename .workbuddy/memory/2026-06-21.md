# 2026-06-21 工作日志

## 任务：诊断所有优化器效果低于 baseline 的根因

### 背景
benchmark 显示所有 12 个优化器变体（ape/opro/dspy/promptbreeder/evoprompt/spo 各 2 个别名）的 accuracy 均低于 baseline 0.85，最好的 spo-optimizer 只有 0.815（-3.5pp）。部分方法耗时极高（SPO 24000+ 秒）。

### 诊断结论

baseline 和优化器走两条不同的评估链路，优化器链路存在 3 个关键 bug + 2 个设计缺陷：

**P0 致命 Bug（直接解释 6pp 差距）：**
1. **`{input}` 占位符未替换** — `src/prompt_evolution/evaluation/evaluator.py:123`
   - 初始 prompt 模板 `新闻标题："{input}"\n类别：` 需要 `replace("{input}", title)`
   - baseline (`benchmark_news.py:265`) 正确做了替换 → 0.85
   - 优化器走 `Evaluator._generate_predictions`，直接 `f"{instruction}\n\n用户输入：{user_input}"`，`{input}` 字面量原样发给 LLM

2. **instruction 双发** — `evaluator.py:125-130`
   - `system_prompt=prompt.instruction` + `prompt=full_prompt`（含 instruction），LLM 收到 instruction 两次

3. **输出格式引导被破坏** — 初始 prompt 末尾 `\n类别：` 被追加的 `\n\n用户输入：xxx` 打乱，配合 AccuracyMetric 严格 `==` 比较导致误判

**P0 设计缺陷：**
4. 候选 prompt 生成时未约束保留 `{input}` 占位符 — APE/OPRO/SPO 改写 prompt 时可能丢掉占位符

**P1：**
5. Evaluator 无并发，顺序 `for item in dataset` — SPO 14400 次串行调用 → 24000 秒

**P2：**
6. 训练集仅 200 条，评估方差大
7. 初始 prompt 已强（0.85），优化空间有限

### 修复优先级
- P0：修 Evaluator 三处 bug + 候选生成约束 → 优化器准确率应追平 baseline
- P1：Evaluator 加并发 → SPO 耗时 24000s → ~2000s
- P2：扩训练集 / 换更难数据集

### 关键文件
- `src/prompt_evolution/evaluation/evaluator.py` — 评估器核心（P0 主修目标）
- `src/prompt_evolution/optimizers/ape/optimizer.py` — APE 候选生成
- `src/prompt_evolution/optimizers/opro/optimizer.py` — OPRO 候选生成
- `src/prompt_evolution/optimizers/spo/optimizer.py` — SPO 候选生成
- `benchmark_news.py:215-312` — baseline 评估逻辑（正确参考实现）

### 下一步
用户已确认走"先诊断效果差距"路径。诊断报告已交付，待用户确认是否动手修 P0。

---

## 修复实施（P0 + P1）

### 改动文件
1. `src/prompt_evolution/evaluation/evaluator.py` — P0 主修
   - `__init__` 加 `concurrency: int = 5` 参数
   - `_generate_predictions` 重写：
     - **占位符替换**：检测 `{input}`，走 `instruction.replace("{input}", user_input)`，与 baseline 对齐
     - **不再传 system_prompt**：消除 instruction 双发
     - **并发**：`asyncio.Semaphore(self._concurrency)` + `asyncio.gather`
     - few-shot 加在 instruction 之前，不破坏末尾输出引导
2. `src/prompt_evolution/optimizers/ape/optimizer.py`
   - 加 `_placeholder_constraint` 配置项，生成 prompt 时强制要求保留 `{input}`
   - padding 兜底：变体标记放 instruction 之前（不破坏末尾 `\n类别：`）
3. `src/prompt_evolution/optimizers/opro/optimizer.py`
   - `_CANDIDATE_GEN_SYSTEM` 加 CRITICAL 约束保留 `{input}`
   - padding 兜底：换成含 `{input}` 的安全模板（之前的 padding 完全无占位符）
4. `src/prompt_evolution/optimizers/spo/optimizer.py`
   - `_SEMANTIC_REWRITE_SYSTEM` + `_GUIDED_SEARCH_SYSTEM` 加 CRITICAL 约束保留 `{input}`
   - rewrite fallback：变体标记放 instruction 之前

### 验证
- `pytest tests/` 9 个测试全过
- `scripts/verify_evaluator_fix.py --eval-samples 50` 实测：
  - baseline 链路：0.7800
  - 修复后 Evaluator 链路：0.7800
  - **差值 +0.0000** —— 两条链路完全一致，P0 bug 已消除
- 修复后 evaluator 还更快（20.1s vs 27.3s），并发实现更干净

### 未做（后续）
- DSPy / PromptBreeder / EVOPrompt 三个优化器的 fallback 检查（benchmark 里跑的是 APE/OPRO/SPO，先验证这三个）
- 跑完整 benchmark 看 APE 等是否能真正超过 baseline（修复了评估链路后才有意义）
- 扩训练集 / 换更难数据集（P2）

### 关键洞察
- benchmark 里 baseline 0.85 vs 优化器 0.78-0.815 的差距，**6pp 中绝大部分是评估链路 bug 导致的虚假差距**，不是优化器本身差
- 修复后优化器与 baseline 走同一条评估链路，才能真正比较"优化器是否提升了 prompt"

---

## DSPy/PromptBreeder/EVOPrompt 同类问题诊断（未修复）

用户要求先看这三个优化器有没有同类 fallback 问题。诊断结论：

### 共性问题（与 APE/OPRO/SPO 相同模式）
1. **P0 占位符丢失**：三个优化器的所有候选生成 system prompt（DSPy 的 `_PROPOSE_SYSTEM`；PB/EVO 的 `_MUTATION_SYSTEM`/`_CROSSOVER_SYSTEM`/`_INIT_VARIANT_SYSTEM`）均未约束保留 `{input}`
2. **P1 padding 破坏末尾输出引导**：
   - DSPy `optimizer.py:350-357`：`current_best_instruction + f" (variation ...)"`
   - PromptBreeder `optimizer.py:341-348`：`base_prompt.instruction + f" (variant ...)"`
   - EVOPrompt `optimizer.py:366-374`：`base_prompt.instruction + f" [variant ...]"`
   - 三处都把变体标记**追加到 instruction 末尾**，破坏 `\n类别：` 输出引导

### DSPy 独有的 P0 崩溃 bug
- `optimizer.py:263`：`await self.model_provider.agenerate(messages=messages, ...)`
- `LiteLLMProvider` / `BaseModelProvider` 接口**没有 agenerate 方法**，只有 `generate(prompt, system_prompt, ...)`
- AttributeError 被 line 265 except 吞掉 → bootstrap 永远返回空 few_shots → DSPy 退化成纯 propose，丢失核心 few-shot 采样本领
- 修复方式：改用 `generate(prompt=inp, system_prompt=instruction, temperature=0.0)`，与 provider 接口对齐

### 无问题的部分
- PB/EVO 的 `_mutate`/`_crossover` fallback 用 `parent.instruction`，保留占位符 ✅

### 待决策
诊断完成，待用户确认是否动手修。修复模式与上轮 APE/OPRO/SPO 完全一致。

---

## DSPy/PromptBreeder/EVOPrompt 修复实施

### 改动文件
1. `src/prompt_evolution/optimizers/evoprompt/optimizer.py`
   - `_MUTATION_SYSTEM` / `_CROSSOVER_SYSTEM` / `_INIT_VARIANT_SYSTEM` 三个 system prompt 加 CRITICAL 约束保留 `{input}`
   - `_generate_variants` padding 兜底：变体标记移到 instruction 之前
2. `src/prompt_evolution/optimizers/prompt_breeder/optimizer.py`
   - 同上三个 system prompt 加约束
   - `_generate_variants` padding 兜底：变体标记移到 instruction 之前
3. `src/prompt_evolution/optimizers/dspy_optimizer/optimizer.py`
   - **P0 崩溃修复**：`_bootstrap` 不再调用不存在的 `agenerate(messages=...)`，改用 `generate(prompt=, system_prompt=None)`，与 provider 接口对齐
   - **bootstrap 与评估链路对齐**：`_bootstrap` 走占位符替换路径（`instruction.replace("{input}", inp)`），与 evaluator 和 baseline 完全一致 —— 这样收集的"正确样本"才能真实反映当前 prompt 在评估链路下的表现
   - **消除 instruction 双发**：bootstrap 不再传 `system_prompt=prompt.instruction`，避免重复
   - `_PROPOSE_SYSTEM` 加 CRITICAL 约束保留 `{input}`
   - `_propose` padding 兜底：变体标记移到 instruction 之前

### 验证
- 三个优化器导入检查通过
- `pytest tests/` 9 个测试全过
- 至此 6 个优化器全部修复完成：APE / OPRO / SPO（上轮）+ DSPy / PromptBreeder / EVOPrompt（本轮）

### 关键洞察
- DSPy 的 `agenerate` 崩溃被 except 吞掉，导致 bootstrap 永远返回空 few_shots —— DSPy 退化成纯 propose，丢失了核心 few-shot 采样本领。这是 DSPy 在 benchmark 里只有 0.79 的主因。
- 修复后 DSPy 的 bootstrap 才能真正工作：用当前 prompt 推理 → 收集正确样本 → 喂给 propose 生成更好的 instruction

---

## 全面代码 Review（输出 docs/CODE_REVIEW.md）

对整个项目（src/ 33 个 .py ~4055 行 + tests + benchmark + scripts）做了一次全面 review，发现前几轮未捕获的新问题：

### 新发现的 P0（影响正确性）
1. **PromptBreeder / EvoPrompt 候选评估失效（dead code）** — `optimizers/prompt_breeder/optimizer.py:175,190` + `optimizers/evoprompt/optimizer.py:187`
   - 用 `if candidate.score is None:` 判断是否已评估，但 `PromptCandidate.score` 默认 `0.0`（float 非 Optional），**永远不等于 None**
   - 后果：变异/交叉产生的子代从未被评估，全部停留在 0.0；最终只能选出初始 prompt
   - 已用 `uv run python -c` 复现确认：`PromptCandidate(id='x', instruction='hi').score is None → False`
   - 这解释了 benchmark 里 promptbreeder 耗时仅 848s（SPO 24320s）——几乎没调评估器
   - 修复：score 改 `Optional[float]=None`，或加 `evaluated` 标记字段

2. **benchmark 训练集/测试集口径混比** — `benchmark_news.py:491-501`
   - baseline 报测试集准确率，优化器报 `result.best_prompt.score`（训练集准确率），二者并列对比
   - "都不如 baseline" 的结论部分源于此口径错误
   - 修复：每个优化器用 best_prompt 在测试集上再评测一次，与 baseline 同口径

### 新发现的 P1
3. **APE 费用统计虚高** — `optimizers/ape/optimizer.py:112` 循环里 `total_cost += 累计值`（平方级虚高）；其余优化器用增量法（`cost_after - cost_before`）是对的
4. **F1ScoreMetric 用 set 而非 Counter** — `evaluation/metrics/f1_score.py:34-35`，丢词频，不是标准 F1
5. **providers/base.py 重复定义 BaseModelProvider** — 与 core/base.py 平行定义，litellm_provider 实际继承 core 版本，二义性
6. **Evaluator.evaluate_batch 用 asyncio.run** — `evaluation/evaluator.py:102`，在已有循环内会崩；且忽略 parallel 参数
7. **UI 评估 Tab 调用 Evaluator 接口错误** — `ui/app.py:246` `evaluator.evaluate(predictions, references)` 签名不符 + 未 await + 期望 dict 却得 float

### 新发现的 P2
8. OPRO 历史取 `sorted_history[-20:]` = 分数最低 20 条（与注释相反）— `optimizers/opro/optimizer.py:298`
9. DSPy 早停 `range(1,4)` 访问 `_scores[-4]` 在 len==3 时越界回绕 — `optimizers/dspy_optimizer/optimizer.py:196`
10. UI `_run_prompt_on_dataset` 丢弃 prompt 模板（把 input 当 prompt、prompt 当 system）— `ui/app.py:96-112`
11. `litellm.drop_params=True` 全局副作用、`random.seed(42)` 全局污染
12. 测试覆盖：7 个优化器仅 APE 有测试；OPRO/DSPy/PB/EVO/SPO 零测试（P0-1 本该被测试拦住）
13. README 称依赖 dspy-ai 但 dspy_optimizer 实为轻量实现无 dspy 依赖；优化器状态描述过时

### 亮点
- 抽象设计干净（BaseOptimizer/Provider/Evaluator/Metric 四件套 + 工厂注册表）
- `{input}` 占位符链路统一（前几轮已修复）
- LiteLLMProvider base_url 解析周到且有测试
- benchmark 断点续评 + 分层采样实用
- 文档论文引用齐全

### 评分
架构 A- / 正确性 C / 测试覆盖 D。完整报告见 `docs/CODE_REVIEW.md`。

---

## P0 修复实施

### P0-1: PromptBreeder / EvoPrompt 候选评估失效

**根因**：`if candidate.score is None:` 恒为 False（`PromptCandidate.score` 默认 0.0，是 float 非 Optional），导致变异/交叉子代从未被评估。

**修复方案**：给 `PromptCandidate` 加 `evaluated: bool = False` 标记字段，在 `Evaluator.evaluate` 内部评估后自动设置 `prompt.evaluated = True`，进化算法改用 `if not candidate.evaluated:` 判断。

**改动文件**：
1. `src/prompt_evolution/core/models.py` — PromptCandidate 加 `evaluated: bool = False` 字段
2. `src/prompt_evolution/evaluation/evaluator.py` — `evaluate()` 返回前设置 `prompt.score = final_score; prompt.evaluated = True`（原地回写，所有优化器自动受益）
3. `src/prompt_evolution/optimizers/prompt_breeder/optimizer.py`
   - `unevaluated = [p for p in population if p.score is None]` → `if not p.evaluated`
   - 删除冗余的第二个评估循环（dead code）
   - 主循环结束后加"最终评估"：对 all_candidates 里所有未评估候选（最后一轮子代）做一次评估，确保 best_prompt 选择公平、num_candidates_evaluated 准确
   - `num_candidates_evaluated` 统计改用 `c.evaluated`
4. `src/prompt_evolution/optimizers/evoprompt/optimizer.py` — 同上三处改动

**验证**：
- 9 个现有测试全过
- `scripts/verify_p0_fix.py` 用 mock provider 验证：修复前子代 score 全 0.0、best 选 initial；修复后所有候选 evaluated=True，best_prompt score=1.0 且选到含 MAGIC 关键词的高分子代

### P0-2: benchmark 训练集/测试集口径混比

**根因**：baseline 报测试集准确率，优化器报 `result.best_prompt.score`（训练集得分），二者并列对比导致"优化器都不如 baseline"结论失真。

**修复**：`benchmark_news.py` 优化器循环重写：
1. 每个优化器跑完后，用 `best_prompt.instruction` 在 `test_data` 上调用 `evaluate_prompt` 评测（与 baseline 同口径），用独立的 `method_name=f"{method}__test"` 做 checkpoint key
2. `results` 里 `score` = 测试集准确率（主指标），新增 `train_score` 字段（参考指标）
3. checkpoint 改用 `test_accuracy` 字段；跳过逻辑只在 `test_accuracy` 存在时才完全跳过（旧 checkpoint 只有 `accuracy`=训练集分数，会被重跑——这是正确的，旧数据口径错了）
4. 汇总表显示 `Test Acc` + `Train Acc` 两列，并加注释说明口径

**验证**：语法检查 + 导入检查 + 关键改动点断言全部通过

### 关键洞察
- P0-1 是前几轮诊断漏掉的 bug：之前只修了 evaluator 链路（占位符/双发/并发）和候选生成的 `{input}` 约束，但没发现进化算法的评估判断条件本身是 dead code
- 修复后重跑 benchmark，PromptBreeder/EvoPrompt 的耗时会显著增加（因为现在真的在评估子代），效果也可能改变——"都不如 baseline"的结论需要重新验证
- P0-2 修复后，所有优化器与 baseline 走同一测试集口径，对比才有意义

### 改动文件清单
- `src/prompt_evolution/core/models.py`
- `src/prompt_evolution/evaluation/evaluator.py`
- `src/prompt_evolution/optimizers/prompt_breeder/optimizer.py`
- `src/prompt_evolution/optimizers/evoprompt/optimizer.py`
- `benchmark_news.py`
- `scripts/verify_p0_fix.py`（新增验证脚本）

